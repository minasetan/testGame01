# Flag Runner

Phaser.js で作成した、GitHub Pages で公開できる静的な横スクロールアクションゲームです。

## 操作

- `A` / `D`: 左右移動
- `W` / `Space`: ジャンプ
- `R`: リスタート

## 公開方法

GitHub にこのリポジトリを push したあと、GitHub の `Settings` -> `Pages` で公開元を `main` ブランチのルートに設定してください。

## ローカルで確認

静的ファイルだけで動くため、`index.html` をブラウザで開いて確認できます。
